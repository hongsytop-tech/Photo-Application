# `.env` 로 복사해서 값을 채우세요. .env 는 git-ignore 됩니다.
# flutter_dotenv 가 asset 으로 번들해 런타임에 읽습니다.

# Supabase 프로젝트 자격증명 (Project Settings → API)
# 비워두면 앱은 "로컬 전용 모드"로 정상 동작합니다. 로그인/동기화만 꺼집니다.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# 주의: 이 앱은 사진 원본을 서버로 올리지 않습니다.
# 동기화 대상은 메모·태그·가상 폴더 같은 텍스트 메타데이터뿐입니다.
